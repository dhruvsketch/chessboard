# TODO

- [x] Fix invalid viewport meta in `chessboard.html`.
- [x] Fix invalid CSS (`text-shadow`) and make board responsive via CSS Grid.
- [x] Harden click/move logic in `chess.js` (avoid overwriting pieces; avoid stuck selection).
- [x] Integrate minimax with configurable `depth`.
- [x] Fix minimax/turn-handling by tracking `turn`.
- [x] Make moves “proper chess” (legal move gen: check rules, castling, en-passant, promotion, checkmate/stalemate).
- [x] Ensure UI allows legal captures and promotion choice (Q/R/B/N).


